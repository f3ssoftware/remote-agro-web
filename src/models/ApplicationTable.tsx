export class ApplicationTable {
  application_tables?: {user_product_id: number,
  test: number,
  quantity: number,
  tank: number,
  applied: number,
  application_id: number,
  total_quantity: number}[]
}
